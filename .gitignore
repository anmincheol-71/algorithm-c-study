# 컴파일 실행 파일 (확장자 없는 파일들)
# 리눅스에서 보통 실행 파일은 확장자가 없으므로 아래와 같이 설정하거나
# 특정 실행 파일 이름을 직접 지정합니다.
a.out
*.out

# 객체 파일 및 빌드 결과물
*.o
*.obj

# OS 관련 임시 파일
.DS_Store
Thumbs.db

# (선택) 빌드 디렉토리를 따로 쓴다면
build/
bin/
